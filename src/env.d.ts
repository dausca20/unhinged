/// <reference types="expo/types" />

// Brings in Expo's ambient types, including a typed `process.env` for
// EXPO_PUBLIC_* variables and the `__DEV__` global. Committed (unlike the
// auto-generated root expo-env.d.ts) so a clean checkout typechecks without
// first running `expo start`.
